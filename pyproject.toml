[project]
name = "scaling-poisoning"
description = "Scaling laws for data poisoning"
requires-python = ">=3.10"
version="0.0.1"

dependencies = [
    "aim>=3.4.0",
    "fastapi",
    "datasets",
    "multiple-inference",
    "python-dotenv",
    "openai",
    "peft",
    "sentencepiece",
    "statsmodels",
    "torch",
    "transformers[accelerate]",
    "trl",
    "bitsandbytes",
    "lm-eval",
    "uvicorn[standard]",
]

[project.optional-dependencies]
dev = [
    "black",
    "python-dotenv",
    "isort",
    "jupyter",
    "names_generator",
    "seaborn",
]

[tool.setuptools.packages.find]
namespaces = false

[tool.isort]
profile = "black"